apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-gateway-test
  namespace: test-ns
  labels:
    app: test-gateway-test-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-gateway-test-label
  template:
    metadata:
      labels:
        app: test-gateway-test-label
    spec:
      imagePullSecrets:
      - name: dockerhub-secret
      containers:
      - name: test-gateway-test
        image: docker.io/dylee00/gateway-test:latest
        ports:
        - containerPort: 8081
