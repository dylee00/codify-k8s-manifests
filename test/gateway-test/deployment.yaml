apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-gateway-test
  namespace: test-ns
  labels:
    app: test-gateway-test-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-gateway-test-label
  template:
    metadata:
      labels:
        app: test-gateway-test-label
    spec:
      containers:
      - name: test-gateway-test
        image: dylee00/gateway-test:
        ports:
        - containerPort: 8081
