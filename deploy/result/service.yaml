apiVersion: v1
kind: Service
metadata:
  name: codify-resultd
  namespace: codify
spec:
  selector:
    app: codify-result-label
  ports:
  - name: codify-result-port
    port: 80
    targetPort: 8080
  type: NodePort
