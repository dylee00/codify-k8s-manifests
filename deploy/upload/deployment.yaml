apiVersion: apps/v1
kind: Deployment
metadata:
  name: codify-upload
  namespace: codify
  labels:
    app: codify-uploadd-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codify-uploadd-label
  template:
    metadata:
      labels:
        app: codify-upload-label
    spec:
      containers:
        - name: codify-upload
          image: dylee00/codify-upload:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: codify-secret
