apiVersion: apps/v1
kind: Deployment
metadata:
  name: codify-submit
  namespace: codify
  labels:
    app: codify-submit-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codify-submit-label
  template:
    metadata:
      labels:
        app: codify-submit-label
    spec:
      containers:
        - name: codify-submit
          image: dylee00/codify-submit:latest
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: codify-secret
