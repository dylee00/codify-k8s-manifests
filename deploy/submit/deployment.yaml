apiVersion: apps/v1
kind: Deployment
metadata:
  name: codify-submit
  namespace: codify
  labels:
    app: codify-submit-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codify-submit-label
  template:
    metadata:
      labels:
        app: codify-submit-label
    spec:
      containers:
        - name: codify-submit
          image: dylee00/codify-submit:17407850882
          ports:
            - name: http
              containerPort: 8080
            - name: actuator
              containerPort: 9090
          envFrom:
            - secretRef:
                name: codify-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: actuator
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
