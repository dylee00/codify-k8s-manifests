apiVersion: v1
kind: Service
metadata:
  name: codify-dashboard
  namespace: codify
spec:
  selector:
    app: codify-dashboard-label
  ports:
  - name: codify-dashboard-port
    port: 80
    targetPort: 8080
  type: NodePort
