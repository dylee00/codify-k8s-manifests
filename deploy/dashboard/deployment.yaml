apiVersion: apps/v1
kind: Deployment
metadata:
  name: codify-dashboard
  namespace: codify
  labels:
    app: codify-dashboard-label
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codify-dashboard-label
  template:
    metadata:
      labels:
        app: codify-dashboard-label
    spec:
      containers:
        - name: codify-dashboard
          image: dylee00/codify-dashboard:17408000285
          ports:
            - name: http
              containerPort: 8080
            - name: actuator
              containerPort: 9090
          envFrom:
            - secretRef:
                name: codify-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: actuator
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3 
