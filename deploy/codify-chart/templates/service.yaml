apiVersion: v1
kind: Service
metadata:
  name: {{ include "codify-chart.fullname" . }}
  labels:
    {{- include "codify-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codify-chart.selectorLabels" . | nindent 4 }}
